<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/rootLayout"
    android:fitsSystemWindows="true">

    <!-- Background Image -->
    <ImageView
        android:id="@+id/backgroundImage"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@drawable/bg_quest_session"
        android:scaleType="centerCrop"
        android:contentDescription="Background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Gradient Overlay -->
    <View
        android:id="@+id/gradientOverlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/gradient_overlay"
        android:alpha="0.7"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Back Button -->
    <ImageView
        android:id="@+id/backButton"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_margin="16dp"
        android:background="@drawable/icon_bg"
        android:clickable="true"
        android:focusable="true"
        android:contentDescription="Back"
        android:padding="10dp"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        app:tint="#FFFFFF"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Pet Display with Mood Indicator -->
    <FrameLayout
        android:id="@+id/petContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="60dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Mood Indicator Circle -->
        <View
            android:id="@+id/moodIndicator"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_gravity="center"
            android:background="@drawable/circle_pulse"
            android:alpha="0.3" />

        <!-- Pet Display -->
        <ImageView
            android:id="@+id/petDisplay"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_gravity="center"
            android:src="@drawable/emotion_neutral"
            android:contentDescription="Virtual Companion" />
    </FrameLayout>

    <!-- Quest Info Card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/questInfoCard"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="24dp"
        app:cardCornerRadius="20dp"
        app:cardElevation="8dp"
        app:cardBackgroundColor="#FCE0C3"
        app:layout_constraintTop_toBottomOf="@id/petContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- Mood Badge -->
            <TextView
                android:id="@+id/moodBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="CALM"
                android:textSize="11sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:background="@drawable/mood_badge_bg"
                android:paddingHorizontal="14dp"
                android:paddingVertical="5dp"
                android:fontFamily="@font/nunito_bold"
                android:letterSpacing="0.1" />

            <!-- Quest Title -->
            <TextView
                android:id="@+id/questTitleText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="Quest Title"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#2D3748"
                android:fontFamily="@font/nunito_bold"
                android:gravity="center" />

            <!-- Quest Description -->
            <TextView
                android:id="@+id/questDescriptionText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Quest description goes here"
                android:textSize="14sp"
                android:textColor="#4A5568"
                android:fontFamily="@font/nunito_regular"
                android:gravity="center"
                android:lineSpacingExtra="2dp" />

            <!-- Divider -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="18dp"
                android:layout_marginBottom="16dp"
                android:background="#FAD1A8" />

            <!-- Timer Display -->
            <TextView
                android:id="@+id/timerText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="05:00"
                android:textSize="48sp"
                android:textStyle="bold"
                android:textColor="#4CAF50"
                android:fontFamily="@font/nunito_bold"
                android:gravity="center" />

            <!-- Instruction Text -->
            <TextView
                android:id="@+id/instructionText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Focus on your task..."
                android:textSize="13sp"
                android:textColor="#718096"
                android:fontFamily="@font/nunito_regular"
                android:gravity="center" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Action Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/actionButton"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_marginHorizontal="32dp"
        android:layout_marginBottom="20dp"
        android:enabled="false"
        android:fontFamily="@font/nunito_bold"
        android:text="In Progress..."
        android:textSize="17sp"
        android:textStyle="bold"
        app:backgroundTint="#4CAF50"
        app:cornerRadius="30dp"
        app:elevation="6dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>